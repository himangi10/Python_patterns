for row in range (6):
    print()
    for col in range (7):
        if ((col==0 and row<=2) or (col==6 and row<=2) or (row-col==2) or (row==col==4) or (row==3 and col==5)):
            print("*",end=" ")
        else:
            print(" ",end=" ")
